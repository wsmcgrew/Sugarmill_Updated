class Grower{
    constructor(MillId, MillCr, GrowerId, GrowerName, TractId, TractName, HaulerId, HaulerName, TruckId, TailerId, GrossWt, TareWt, 
                NetWt, GrossTime, TareTime, Overweight, id){
        this.MillId
    }
}

//Grower.associate = function(models);

module.exports = Grower;
